# Loss

**L1 Loss (平均绝对误差)**

$$\mathcal{L} = ||\hat{y} - y||_1$$

\$$L\$$**(绝对误差) 对异常值不敏感;**

适配方向：稠密几何回归（深度、光线、梯度）深度与光线预测：预测深度图（Depth）和射线图（Ray）时，全部使用 \$$L\_\$$ 损失。相比\$$L\$$，能产生更清晰的边缘，防止预测结果过度平滑。梯度约束：在计算深度梯度损失（Gradient Loss）时，通常使用 \$$L\_\$$ 范数来计算预测梯度与真值梯度的差异，以保持物体轮廓的锐利度。
